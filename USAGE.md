# Usage Guide

This project is streamlined using a `Makefile` that handles building, environment sourcing, and terminal management.

## 1. Running the Full Simulation
To build the workspace and launch Gazebo alongside all control nodes in a multi-pane Tilix window, execute:
```bash
make sim
